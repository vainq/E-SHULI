import AdSlot from './AdSlot';import { ADS } from '../lib/ads';import { useState } from 'react';export default function StickyBannerAd(){const[hidden,setHidden]=useState(false);if(!ADS.sticky||hidden)return null;return(<div className='sticky top-0 z-40 bg-white/80 backdrop-blur border-b'><div className='container mx-auto px-3 py-2 flex items-start gap-3'><div className='flex-1'><AdSlot slot={ADS.sticky}/></div><button aria-label='Hide ad' className='text-xs px-2 py-1 border rounded hover:bg-gray-50' onClick={()=>setHidden(true)}>×</button></div></div>);}
